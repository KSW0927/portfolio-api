# 앱 서버(오라클 클라우드)에서 docker-compose.prod.yml과 같은 폴더에 .env로 저장해서 사용
DB_HOST=10.0.0.107
DB_USERNAME=postgres
DB_PASSWORD=
JWT_SECRET=
CORS_ALLOWED_ORIGIN=

# Redis/Kafka가 DB_HOST와 다른 서버로 분리되면 여기 채우기 - 비워두면 DB_HOST를 그대로 씀(지금처럼 한 서버에 다 있을 때)
# INFRA_HOST=
