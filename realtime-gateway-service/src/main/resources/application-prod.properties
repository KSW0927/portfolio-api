# 배포(오라클 클라우드)용 설정 - 기본값 없이 전부 환경변수로 주입받음

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS}
spring.kafka.consumer.group-id=realtime-gateway-service

# CORS/WebSocket 허용 origin - 실제 배포된 프론트 도메인
cors.allowed-origin=${CORS_ALLOWED_ORIGIN}
